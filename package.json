{
  "name": "vk-chat-bot",
  "version": "10.5.1",
  "description": "Framework for easy creation of chat bots for VK communities (uses Callback API).",
  "keywords": [
    "api",
    "bot",
    "callback",
    "chat",
    "community",
    "conversation",
    "es6",
    "group",
    "message",
    "sdk",
    "vk"
  ],
  "bugs": "https://github.com/u32i64/vk-chat-bot/issues",
  "repository": {
    "type": "git",
    "url": "https://github.com/u32i64/vk-chat-bot.git"
  },
  "license": "MIT",
  "author": {
    "name": "Artem Varaksa (u32i64)",
    "email": "aymfst@gmail.com"
  },
  "files": [
    "dist/vk-chat-bot.min.js",
    "dist/vk-chat-bot.min.js.map",
    "LICENSE",
    "CHANGELOG.md",
    "README.md"
  ],
  "main": "dist/vk-chat-bot.min.js",
  "scripts": {
    "build": "yarn run clean && rollup -c",
    "clean": "rm -rf dist *.tgz && yarn run clean:docs",
    "clean:docs": "rm -rf docs",
    "minify": "uglifyjs dist/vk-chat-bot.js -c -o dist/vk-chat-bot.min.js --source-map 'content=inline'",
    "prepare": "yarn run test && yarn run build && yarn run minify && yarn run docs",
    "test": "yarn run test:lint && yarn run test:ava",
    "test:ava": "ava",
    "test:lint": "standard --verbose | snazzy",
    "fix": "standard --fix",
    "docs": "yarn run clean:docs && jsdoc -c .jsdoc.json --private && mv ./docs/vk-chat-bot/*/* ./docs/ && rm -rf ./docs/vk-chat-bot"
  },
  "dependencies": {
    "@babel/polyfill": "^7.0.0",
    "@babel/runtime": "^7.0.0",
    "body-parser": "^1.18.3",
    "colors": "^1.3.2",
    "express": "^4.16.3",
    "jsdoc": "^3.5.5",
    "minami": "^1.2.3",
    "moment": "^2.22.2",
    "moment-duration-format": "^2.2.2",
    "request": "^2.88.0",
    "request-promise": "^4.2.2"
  },
  "devDependencies": {
    "@babel/core": "^7.0.1",
    "@babel/plugin-transform-runtime": "^7.0.0",
    "@babel/preset-env": "^7.0.0",
    "@babel/register": "^7.0.0",
    "ava": "^1.0.0-beta.8",
    "rollup": "^0.65.2",
    "rollup-plugin-babel": "^4.0.3",
    "rollup-plugin-node-resolve": "^3.4.0",
    "snazzy": "^8.0.0",
    "standard": "^12.0.1",
    "uglify-js": "^3.4.9"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "ava": {
    "require": [
      "@babel/register"
    ]
  },
  "standard": {
    "ignore": [
      "docs/"
    ]
  }
}